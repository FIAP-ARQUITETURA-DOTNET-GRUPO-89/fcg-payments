name: 🐳 Publish FcgPayments Images

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - run: dotnet restore
      - run: dotnet build --configuration Release --no-restore
      - run: dotnet test --configuration Release --no-build

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Get Version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Publish Payments API
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./src/FcgPayments.Api/Dockerfile
          push: true
          tags: |
            jondamiao/fcg-payments-api:${{ env.VERSION }}
            jondamiao/fcg-payments-api:latest

      - name: Publish Payments Worker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./src/FcgPayments.Worker/Dockerfile
          push: true
          tags: |
            jondamiao/fcg-payments-worker:${{ env.VERSION }}
            jondamiao/fcg-payments-worker:latest
